<%= form_for(@machine_info_type) do |f| %>
  <% if @machine_info_type.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_info_type.errors.count, "error") %> prohibited this machine_info_type from being saved:</h2>

      <ul>
      <% @machine_info_type.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>

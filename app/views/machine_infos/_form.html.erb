<%= form_for(@machine_info) do |f| %>
  <% if @machine_info.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@machine_info.errors.count, "error") %> prohibited this machine_info from being saved:</h2>

      <ul>
      <% @machine_info.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
